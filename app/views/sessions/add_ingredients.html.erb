<!-- Add ingredients -->
<div class="page-header">
  <h2>Add a recipe</h2>
  <h3>Step 2: Add ingredients</h3>
</div>

<div class="row">
  <div class="col-md-12">

    <form action="/create_combination/<%=@recipe.id%>" method="post">
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">
        <div class="form-group">
        <input type="hidden" id="recipe" name="recipe_id" class="form-control" value="<%=@recipe.id%>">
          <div class="checkbox">
            <% @ingredients.each do |ingredient| %>
          <ul>
            <label>
              <input type="checkbox" id="ingredient_id" name="ingredient_id[]" value="<%=ingredient.id %>" >
              <li>
                <%= ingredient.name %>
              </li>
              </ul>
              <% end %>
            </label>
          </div>

      <button class="btn btn-success">
        Submit recipe
      </button>
      or
      <a href="/recipes/new">Cancel</a>
        </div>
    </form>
    </div>
</div>

<div class="row">
  <div class="col-md-12">
    <h5>*If you can't find your ingredients in the list, add them <a href="/ingredients">here</a></h5>
  </div>
</div>
